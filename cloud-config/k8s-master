#cloud-config
package_update: true
packages:
  - docker
runcmd:
  - sudo ip addr add 116.203.9.153 dev eth0
  - systemctl enable docker
  - systemctl start docker
  - sudo docker run -d --privileged --restart=unless-stopped --net=host -v /etc/kubernetes:/etc/kubernetes -v /var/run:/var/run rancher/rancher-agent:v2.2.8 --server https://116.203.61.197 --token xxx --ca-checksum xxx --etcd --controlplane